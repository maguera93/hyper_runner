using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using MAG.Popups;

public class EndScreen : PopupStateModel<EndScreen, PlayerModel>
{


    public override void Setup(PlayerModel playerModel)
    {
        base.Setup(playerModel);
        
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
